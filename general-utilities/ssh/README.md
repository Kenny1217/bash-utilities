# SSH Command Scripts

This folder contains a bash script that allow you to easily execute commands onto a remote server using ssh.

## Scripts

### 1. `ssh_run_command.sh`

This script executes a command on a remote server using ssh.

#### Usage:
```
bash ./ssh_run_command.sh.sh <SSH_USER> <SSH_HOST> <SSH_COMMAND> 
```